{
  "name": "smart-doubt-solver-backend",
  "version": "1.0.0",
  "description": "Backend for Smart Doubt Solver with auto-tagging and similar doubt detection",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install --prefix ../frontend && npm run build --prefix ../frontend",
    "heroku-postbuild": "npm install --prefix ../frontend && npm run build --prefix ../frontend"
  },
  "keywords": [
    "doubt-solver",
    "mern",
    "nlp",
    "similarity"
  ],
  "author": "Midhun",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express-validator": "^7.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
